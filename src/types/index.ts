export type Info = {
    id: string;
    name: string;
    images: string[];
  };

  export type GalleryType = {
    id: string;
    name: string;
    image: string;
    cat: string;
  };
  
  export type LightBoxImagesType = {
    id: string;
    cat: string;
    src: string;
  }[];